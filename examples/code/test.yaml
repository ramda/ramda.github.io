  title: Pick by Index   
  tags: ['a', 'b']
  description: This is a description
  source: |
        let R = require('ramda');

        // :: [Number] -> [a] -> [a]
        var pickIndexes = R.compose(R.values, R.pickAll);
        pickIndexes([0, 2], ['a', 'b', 'c']);